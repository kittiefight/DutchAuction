const Migration = artifacts.require('../contracts/Migrations.sol');

contract('Migration', (accounts) => {
  // let electionInstance;

  // describe('Migration run', () => {
  //   it('should set migration number', () => {
  //     return Migration.deployed().then((instance) => {
  //       instance.setCompleted(2);
  //       return instance.last_completed_migration();
  //     }).then((count) => {
  //       assert.equal(count, 2);
  //     });
  //   });
  // });
  
});
